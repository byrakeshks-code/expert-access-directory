# Dependencies and build outputs
node_modules/
.next/
out/
dist/
build/
coverage/
*.tsbuildinfo
next-env.d.ts

# Environment and secrets (required for VPS: add on server)
.env
.env.*
!.env.example

# GitHub Actions (push needs PAT "workflow" scope; remove this line to add workflows later)
.github/

# OS and IDE
.DS_Store
*.pem
.idea/
.vscode/
.cursor/

# Do not push docs and PDFs (as requested)
*.md
*.pdf

# Plans and local notes (not needed for hosting)
Plans/
*.plan.md
*creds*
*server.txt
updates server.txt
start server.txt
Schema.txt
firebase_web.txt
*.txt
# Track policy page source for the web app (override *.txt above)
!web/content/policy/*.txt

# Supabase local (keep config.toml; ignore local data)
supabase/.temp/
supabase/.branches/

# Local scripts (not needed on VPS)
push-to-github.sh

# Logs and debug
*.log
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
.vercel

# Backend
backend/dist/
backend/coverage/
